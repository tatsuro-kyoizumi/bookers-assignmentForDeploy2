<%= render 'list', user: @user, book: @book %>


<body>
    <div class="body">



        <div class="col-xs-9">
         <h2> Books</h2>
         <table class="table">
         <thead>
          <tr>
            <th></th>
            <th>title</th>
            <th>opinion</th>
            <th colspan="3"></th>
             </tr>
              </thead>

              <% @books.each do |book| %>
              <tbody>
                <tr>
                 <td></td>
          <td><%= attachment_image_tag @user, :profile_image, size: "40x40", format: 'jpeg',fallback: "no_image.jpg" %></td>
          <td><%= link_to book.title, "/books/#{book.id}",class: "book_#{book.id}" %></td>
                            <td><%= book.body %></td>
                        </tr>
                    <% end %>
                  </tbody>
                </table>
            </div>
    </div>
</body>